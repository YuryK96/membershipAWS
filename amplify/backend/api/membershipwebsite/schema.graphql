type Post @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  body: String
  description: String
  readTIme: Int
}

type Testimonial @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  avatar: String
  testimony: String
}
